============
Installation
============

At the command line::

    $ pip install awsssolib

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv awsssolib
    $ pip install awsssolib

Or, if you are using pipenv::

    $ pipenv install awsssolib

Or, if you are using pipx::

    $ pipx install awsssolib
